/**
 * DEPRECATED: this name was misleading.
 *
 * Use `LensFinding` from `app/types/lensFinding` (Lens output DTO; read-only interpretation).
 */
export type {
  LensFinding as EnforcementDecision,
  LensLevel as EnforcementLevel,
  LensAction as EnforcementAction,
  EvidenceMetadata,
  GeoData,
} from "./lensFinding";


